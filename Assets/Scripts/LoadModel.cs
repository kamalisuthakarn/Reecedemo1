using System;
using Siccity.GLTFUtility;
using UnityEngine;
using UnityEngine.Networking;


public class LoadModel : MonoBehaviour
{
    string filePath;

    private void Start()
    {
        // filePath = "Assets/Models/Bath.glb";
        // GameObject model = Importer.LoadFromFile(filePath);
        //Instantiate(model,new Vector3(1,1,1),Quaternion.identity);
    }
}